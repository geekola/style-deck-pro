# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
dist/
build/

# Environment — never commit secrets
.env
.env.local
.env.*.local
.env.development
.env.production

# Vercel
.vercel/

# Drizzle migrations (generated files — commit these)
# lib/db/migrations/  ← intentionally NOT ignored; migrations are committed

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Markdown — project notes, logs, and docs are not tracked
*.md

# source files
fashion-swipe-app.jsx
styledeck-brand-portal.jsx

# stray removed files (OneDrive lock issue prevents deletion)
*.removed

# superseded by row-actions.tsx (OneDrive lock issue prevents deletion)
app/(admin)/admin/brands/brand-actions.tsx

# superseded by row-actions.tsx ship panel (OneDrive lock issue prevents deletion)
app/(brand)/brand/orders/ship-order-button.tsx
